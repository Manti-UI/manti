---
title: Editable
slug: /components/editable
group: Components
order: 27
description: An inline text field that toggles between preview and edit, backed by the Zag.js editable machine.
---

# Editable

An inline text field that toggles between a static preview and an editable input,
backed by the Zag.js editable machine. `activationMode` controls what enters edit
mode and `submitMode` controls what commits the value.

<Demo name="editable/basic" center />

## Props

<PropsTable component="editable" />

## Anatomy

Target these public selectors to customize the field. Class names and the DOM
between parts are private.

<Anatomy component="editable" />

## Component tokens

<TokenTable component="editable" />
